package main

import "fmt" //automatically get imported when we save the file because we installed the tool provided by go team

func main() {
	//any packeage public function name is started by capital letter.
	fmt.Println("Hello Juhi!!!!!") //semicolon is not neccessary lexer will do for that. If we put lexer will remove that.
}
